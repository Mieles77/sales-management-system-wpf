<Page x:Class="Prueba_Apis.Views.HomeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:Prueba_Apis.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HomeView">


    <ScrollViewer VerticalScrollBarVisibility="Auto" materialDesign:ScrollViewerAssist.IsAutoHideEnabled="True">
        <StackPanel Margin="30">
            <materialDesign:Card Background="#7163BA" UniformCornerRadius="20" Padding="30" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="¡Hola de nuevo!" FontSize="28" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Bienvenido a tu panel de control. Aquí tienes un resumen de hoy." 
                               Foreground="#D9CFC7" FontSize="14" Margin="0,5,0,0"/>
                </StackPanel>
            </materialDesign:Card>

            <UniformGrid Columns="2" Margin="0,0,0,20">
                <materialDesign:Card UniformCornerRadius="20" Margin="0,0,10,0" Padding="20" Background="White">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="AtomVariant" Foreground="#7163BA" Width="30" Height="30" VerticalAlignment="Center"/>
                        <StackPanel Margin="15,0,0,0">
                            <TextBlock Text="Productos Vendidos" FontSize="12" Foreground="Gray"/>
                            <TextBlock Text="12 Nuevos" FontSize="18" FontWeight="Bold"/>
                        </StackPanel>
                    </StackPanel>
                </materialDesign:Card>

                <materialDesign:Card UniformCornerRadius="20" Margin="10,0,0,0" Padding="20" Background="White">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Bell" Foreground="#7163BA" Width="30" Height="30" VerticalAlignment="Center"/>
                        <StackPanel Margin="15,0,0,0">
                            <TextBlock Text="Alertas" FontSize="12" Foreground="Gray"/>
                            <TextBlock Text="3 Pendientes" FontSize="18" FontWeight="Bold"/>
                        </StackPanel>
                    </StackPanel>
                </materialDesign:Card>
            </UniformGrid>

            <TextBlock Text="Actividad Reciente" FontSize="20" FontWeight="SemiBold" Margin="0,10,0,15"/>
            <materialDesign:Card UniformCornerRadius="20" Height="400" Background="White">
                <TextBlock Text="Aquí puedes poner una lista de datos o una tabla..." 
                           HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Gray"/>
            </materialDesign:Card>
        </StackPanel>
    </ScrollViewer>
</Page>
