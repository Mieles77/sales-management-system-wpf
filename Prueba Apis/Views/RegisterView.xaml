<Page x:Class="Prueba_Apis.Views.RegisterView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:Prueba_Apis"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RegisterView">

    <Page.Resources>
        <Style TargetType="Button" x:Key="ButtonP">
            <Setter Property="Margin" Value="5" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="#7163BA"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="10" 
                                Background="Transparent" 
                                BorderBrush="#7163BA" 
                                BorderThickness="2">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="ButtonRegister">
            <Setter Property="Margin" Value="5" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="10" 
                                Background="#7163BA" 
                                BorderBrush="#7163BA" 
                                BorderThickness="2">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F5F5F5"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="CanUserResizeColumns" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#7163BA"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" 
                  materialDesign:ScrollViewerAssist.IsAutoHideEnabled="True">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*" MinHeight="300"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- TÍTULO -->
            <TextBlock Grid.Row="0"
                      Text="Gestión de Inventario" 
                      FontSize="28" 
                      FontWeight="Bold" 
                      Foreground="#7163BA" 
                      Margin="0,0,0,20"/>

            <!-- FORMULARIO DE REGISTRO -->
            <materialDesign:Card Grid.Row="1" 
                                UniformCornerRadius="20" 
                                Padding="20" 
                                Margin="0,0,0,20" 
                                Background="White">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- PRIMERA FILA DE INPUTS -->
                    <Grid Grid.Row="0" Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*" MinWidth="150" MaxWidth="180"/>
                            <ColumnDefinition Width="*" MinWidth="180" MaxWidth="250"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Botón Escanear -->
                        <Button Grid.Column="0"
                                Style="{StaticResource ButtonP}" 
                                Margin="0,0,10,0"
                                Command="{Binding EscanearCommand}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="BarcodeScan" 
                                                        Foreground="#7163BA" 
                                                        Width="20" 
                                                        Height="20" 
                                                        Margin="0,0,5,0"/>
                                <TextBlock Text="Escanear" 
                                          FontSize="14" 
                                          Foreground="#7163BA"/>
                            </StackPanel>
                        </Button>

                        <!-- Código -->
                        <TextBox Grid.Column="1"
                                x:Name="txtCodigo"
                                materialDesign:HintAssist.Hint="Código (Opcional)" 
                                Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                Margin="0,0,10,0"
                                local:FocusExtension.IsFocused="{Binding IsCodigoFocused}"
                                Text="{Binding CodigoProducto, UpdateSourceTrigger=PropertyChanged}"/>


                        <!-- Nombre -->
                        <TextBox Grid.Column="2"
                                materialDesign:HintAssist.Hint="Nombre del Producto" 
                                Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                Margin="0,0,10,0"
                                Text="{Binding NombreProducto, UpdateSourceTrigger=PropertyChanged}"/>

                        <!-- Botón Registrar -->
                        <Button Grid.Column="3"
                                Style="{StaticResource ButtonRegister}"
                                Command="{Binding RegistrarProductoCommand}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Plus" 
                                                        Foreground="White" 
                                                        Width="20" 
                                                        Height="20" 
                                                        Margin="0,0,5,0"/>
                                <TextBlock Text="Registrar" 
                                          FontSize="14" 
                                          Foreground="White"/>
                            </StackPanel>
                        </Button>
                        
                    </Grid>

                    <!-- SEGUNDA FILA DE INPUTS -->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" MinWidth="100"/>
                            <ColumnDefinition Width="*" MinWidth="100"/>
                            <ColumnDefinition Width="*" MinWidth="100"/>
                            <ColumnDefinition Width="*" MinWidth="150"/>
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Column="0"
                                materialDesign:HintAssist.Hint="Precio Fábrica" 
                                Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                Margin="0,0,10,0"
                                Text="{Binding PrecioFabrica, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBox Grid.Column="1"
                                materialDesign:HintAssist.Hint="Precio Venta" 
                                Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                Margin="0,0,10,0"
                                Text="{Binding PrecioVenta, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBox Grid.Column="2"
                                materialDesign:HintAssist.Hint="Cantidad" 
                                Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                Margin="0,0,10,0"
                                Text="{Binding Cantidad, UpdateSourceTrigger=PropertyChanged}"/>

                        <DatePicker Grid.Column="3"
                                   materialDesign:HintAssist.Hint="Fecha Vencimiento"
                                   Style="{StaticResource MaterialDesignOutlinedDatePicker}" 
                                   BorderThickness="0"
                                   SelectedDate="{Binding FechaVencimiento}"/>
                    </Grid>
                </Grid>
            </materialDesign:Card>

            <!-- TABLA DE PRODUCTOS -->
            <materialDesign:Card Grid.Row="2" 
                                UniformCornerRadius="20" 
                                Background="White"
                                Padding="10">
                <DataGrid x:Name="dgProductos" 
                         AutoGenerateColumns="False" 
                         CanUserAddRows="False" 
                         Background="Transparent" 
                         BorderThickness="0"                          
                         GridLinesVisibility="None"
                         VerticalScrollBarVisibility="Auto"                          
                         HorizontalScrollBarVisibility="Auto"
                         ItemsSource="{Binding Productos}"
                         SelectedItem="{Binding ProductoSeleccionado}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" 
                                           Binding="{Binding Id}" 
                                           Width="0.5*"
                                           MinWidth="50"/>

                        <DataGridTextColumn Header="Producto" 
                                           Binding="{Binding Nombre}" 
                                           Width="2*"
                                           MinWidth="150"/>

                        <DataGridTextColumn Header="Precio" 
                                           Binding="{Binding PrecioVenta, StringFormat=C}" 
                                           Width="*"
                                           MinWidth="80"/>

                        <DataGridTextColumn Header="Cantidad" 
                                           Binding="{Binding Cantidad}" 
                                           Width="*"
                                           MinWidth="80"/>

                        <DataGridTextColumn Header="Vencimiento" 
                                           Binding="{Binding FechaVencimiento, StringFormat='dd/MM/yyyy'}" 
                                           Width="1.2*"
                                           MinWidth="100"/>

                        <DataGridTemplateColumn Header="Acciones" 
                                               Width="Auto"
                                               MinWidth="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" 
                                               HorizontalAlignment="Center">
                                        <Button Style="{StaticResource MaterialDesignIconButton}" 
                                               Width="30" 
                                               Height="30"
                                               Margin="2"
                                               Command="{Binding EditarProductoCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}">
                                            <materialDesign:PackIcon Kind="Pencil" 
                                                                    Foreground="#7163BA"/>
                                        </Button>
                                        <Button Style="{StaticResource MaterialDesignIconButton}" 
                                               Width="30" 
                                               Height="30"
                                               Margin="2"
                                                Command="{Binding EliminarProductoCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}">
                                            <materialDesign:PackIcon Kind="Delete" 
                                                                    Foreground="Red"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </materialDesign:Card>

            <!-- BOTONES DE IMPORTAR/EXPORTAR -->
            <materialDesign:Card Grid.Row="3" 
                                UniformCornerRadius="20" 
                                Margin="0,20,0,0" 
                                Padding="20"
                                Background="White">
                <WrapPanel Orientation="Horizontal" 
                          HorizontalAlignment="Left">
                    <Button Style="{StaticResource ButtonP}" 
                            Margin="0,0,10,0" Command="{Binding ImportarCommand}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FileImportOutline" 
                                                    Foreground="#7163BA" 
                                                    Width="20" 
                                                    Height="20" 
                                                    Margin="0,0,5,0"/>
                            <TextBlock Text="Importar" 
                                      FontSize="14" 
                                      Foreground="#7163BA"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource ButtonRegister}" Command="{Binding ExportarCommand}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FileExportOutline" 
                                                    Foreground="White" 
                                                    Width="20" 
                                                    Height="20" 
                                                    Margin="0,0,5,0"/>
                            <TextBlock Text="Exportar" 
                                      FontSize="14" 
                                      Foreground="White"/>
                        </StackPanel>
                    </Button>
                    <!--Boton Cancelar-->
                    <Button Grid.Column="3"
                            Style="{StaticResource ButtonP}"
                            Margin="5,0,0,0"
                            Command="{Binding CancelarCommand}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Close" 
                                        Foreground="#7163BA" 
                                        Width="20" 
                                        Height="20" 
                                        Margin="0,0,5,0"/>
                            <TextBlock Text="Cancelar" 
                                          FontSize="14" 
                                          Foreground="#7163BA"/>
                        </StackPanel>
                    </Button>
                </WrapPanel>
            </materialDesign:Card>

            <!-- SNACKBAR -->
            <materialDesign:Snackbar x:Name="Aviso" 
                                    MessageQueue="{materialDesign:MessageQueue}" 
                                    Background="#7163BA" 
                                    Foreground="White"/>
        </Grid>
    </ScrollViewer>
</Page>